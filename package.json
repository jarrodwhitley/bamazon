{
    "name": "bamazon",
    "private": true,
    "type": "module",
    "workspaces": [
        "client",
        "server"
    ],
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "start": "NODE_ENV=development concurrently \"npm run server -w server\" \"npm run dev -w client\"",
        "start-prod": "NODE_ENV=production npm run build -w client && npm run server -w server",
        "server": "node server.js",
        "heroku-logs": "heroku logs --tail --app bamazon",
        "lint": "eslint .",
        "preview": "vite preview",
        "postbuild": "cp CNAME build/ && cp -r favicon build/ && cp -r Procfile build/",
        "production": "NODE_ENV=production npm run build"
    },
    "devDependencies": {
        "glob": "^11.0.1"
    },
    "dependencies": {
        "concurrently": "^9.1.2"
    }
}
